package controllers;

// Koordinatoren for dokumenter.html (US11).
// TODO (Louise): setRoutes(JavalinConfig config) med config.routes.post("/dokumenter", ctx -> uploadDocument(ctx)) – filen læses med ctx.uploadedFile("file")
//   (formularen skal have enctype="multipart/form-data"). Husk at melde til i Main: DocumentController.setRoutes(config)
public class DocumentController {
}
